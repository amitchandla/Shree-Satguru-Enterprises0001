/* =========================================================
   config.example.js
   Copy this file to assets/js/config.js and fill in your
   real Supabase project values (see README.md Step 8).
   assets/js/config.js is listed in .gitignore so your real
   values never reach a public GitHub repository — only the
   ANON key belongs here, never the Service Role key.
   ========================================================= */
window.__SSE_CONFIG__ = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-SUPABASE-ANON-KEY",
};
